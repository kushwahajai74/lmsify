import { AppProviders } from "@/providers/AppProviders";

/**
 * Boilerplate. Everything real lives in `AppProviders` so this file
 * stays trivial and easy to swap (e.g. for an error boundary in FE-7).
 */
export default function App() {
  return <AppProviders />;
}
